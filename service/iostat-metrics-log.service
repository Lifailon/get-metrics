[Unit]
Description=Metrics for iostat from the set sysstat
After=network.target

[Service]
ExecStart=/bin/bash "/root/iostat-metrics-log.sh"
ExecReload=/bin/kill -HUP $MAINPID
KillMode=process
Restart=on-failure
Type=simple

[Install]
WantedBy=multi-user.target